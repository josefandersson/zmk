if SHIELD_ANDERSSON_ALPHA_REV2_LEFT

config ZMK_KEYBOARD_NAME
	default "Alpha r.2"

config ZMK_SPLIT_ROLE_CENTRAL
	default y

endif

if SHIELD_ANDERSSON_ALPHA_REV2_LEFT || SHIELD_ANDERSSON_ALPHA_REV2_RIGHT

config ZMK_SPLIT
	default y

if ZMK_BACKLIGHT

config PWM
	default y

config LED_GPIO
	default y

endif # ZMK_BACKLIGHT

if ZMK_DISPLAY

config I2C
	default y

config SSD1306
	default y

config SSD1306_REVERSE_MODE
	default y

endif

endif
